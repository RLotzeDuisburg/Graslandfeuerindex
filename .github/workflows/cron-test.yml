name: Cron-Test

on:
  schedule:
    - cron: '2/5 * * * *'

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Zeitpunkt ausgeben
        run: |
          echo "Auslöser: ${{ github.event_name }}"
          echo "UTC-Zeit:"
          date -u
