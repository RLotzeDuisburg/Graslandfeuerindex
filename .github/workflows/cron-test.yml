name: Cron-Test

on:
  schedule:
    - cron: '*/5 * * * *'

  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Zeitpunkt ausgeben
        run: |
          echo "Ereignis: ${{ github.event_name }}"
          date -u
